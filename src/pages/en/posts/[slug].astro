---
import type { GetStaticPaths } from "astro";
import RenderedPostPage from "../../../components/RenderedPostPage.astro";
import { getPostStaticPaths } from "../../../utils/page-data";

export const getStaticPaths = (async () => getPostStaticPaths("en")) satisfies GetStaticPaths;
---

<RenderedPostPage post={Astro.props.post} posts={Astro.props.posts} />
